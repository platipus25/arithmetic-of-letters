ArithmeticOfLetters {
  Expr
    = Expr "||" Expr -- concat
    | Expr "+" Char  -- add
    | Expr "-" Char  -- sub
    | Expr "&" Char  -- and
    | Expr "|" Char  -- or
    | Expr "^" Char  -- xor
    | Char

  Char
    = "(" Expr ")"   -- paren
    | any            -- literal
}