//import * as ohm from 'ohm-js';
/*
enum Token {
  OpenParenthesis,
  CloseParenthesis,
  Character,
  Operator,
}


enum OperatorTypes {
  Add,
  Subtract,
  Concat,
}*/

/*class Operator {
  static


  static isOperator(s: string) {
    return
  }
}

class Parser {
  constructor(input: string) {

  }

  parse() {
    for (/)
  }
}
*/


/*const example = `
P + \ || @ - O || (> + |) ^ 7 + G + 0 ^ (- + ^)
`*/